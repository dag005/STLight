#include "stdafx.h"

#include "../STLight/corecrt_wstdio.h"

TEST(_wfopen_sTests, Test1)
{
	/*wchar_t strForComparison[200];
	FILE* filePtr1;

	STLight::_wfopen_s(&filePtr1, L"FilesTests/_wfopen_sTest1.txt", L"r+");

	ASSERT_TRUE(filePtr1 != nullptr);

	fgetws(strForComparison, 200, filePtr1);

	ASSERT_STREQ(L"_wfopen_s Test1", strForComparison);*/
}