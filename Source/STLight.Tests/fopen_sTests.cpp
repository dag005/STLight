#include "stdafx.h"

#include "../STLight/stdio.h"

TEST(fopen_sTests, Test1)
{
	/*char strForComparison[200];
	FILE* filePtr1;

	STLight::fopen_s(&filePtr1, "FilesTests/fopen_sTest1.txt", "r+");

	ASSERT_TRUE(filePtr1 != nullptr);

	fgets(strForComparison, 200, filePtr1);

	ASSERT_STREQ("fopen_s Test1", strForComparison);*/
}